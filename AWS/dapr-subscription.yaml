apiVersion: dapr.io/v2alpha1
kind: Subscription
metadata:
  name: mkpublisher-dapr-subscription
  namespace: default
spec:
  pubsubname: aws-pubsub
  topic: mkpublisher-dapr-topic
  routes:
    default: /messages
scopes:
- mkconsumer
